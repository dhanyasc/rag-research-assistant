global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "rag-research-assistant"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
        labels:
          environment: "production"
          service: "rag-api"
    scrape_interval: 10s
